<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscription</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">
</head>
<body>
    <div class="ui raised segment" id="form_login">
        <% if (danger) { %>
            <div class="ui warning message" id="message">
                <div class="ui header">
                    <p style="text-align: center;"> <%= danger%> </p>
                </div>
            </div>
        <% } %>
        <form action="/changemdp" method="post" class="ui form">
            <h1 class="ui header" style="text-align: center;">Change Password</h1>
            <div class="field">
                <div class="ui left icon input">
                    <input type="password" name="psw" id="psw" autocomplete="new-password" required placeholder="Password">
                    <i class="lock icon"></i>
                </div>
            </div>
            <div class="field">
                <div class="ui left icon input">
                    <input type="password" name="confirm_psw" id="confirm_psw" autocomplete="new-password" required placeholder="Retype Password">
                    <i class="lock icon"></i>
                </div>
            </div>
            <div class="field">
                <button type="submit" class="ui teal button" id="submit_login">Change</button>
            </div>
        </form>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.js"></script>
</body>
</html>